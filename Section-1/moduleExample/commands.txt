1. Run -> go mod init <anyNamespace>/hello
2. Write the code
3. Run -> go mod tidy
4. Run -> go test
5. looks for all mods -> go list -m all
6. Update golang.org/x/text -> go get golang.org/x/text
7. Code change
8. Fetch v3 version of rsc.io/quote -> go get rsc.io/quote/v3
9. code change to migrate
10. for clean up run -> go mod tidy
